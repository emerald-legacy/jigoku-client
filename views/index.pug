extends /layout

block content
  #component
  if token
    script(type="text/javascript").
      var authToken = '#{token}';
  script(type="text/javascript")!= "var user = " + userJson + ";"
  script(type="text/javascript").
    var cardImageVersion = '#{cardImageVersion}';
